'use strict';
    const a=parseFloat(prompt('Please enter a : ',''));
    const b=parseFloat(prompt('Please enter b : ',''));
    if(a<b){
        alert('Maximum is : '+ b);
    }
    else {
    alert('Maximum is : '+ a);
}